# implement scan for rules, i.e. all folders in docs are all lower case
# only ARCHITECTURE.md is allowed to be upper case to put emphasis on it

# Warn if a rule is violated, but don't fail the build since these are just 
# guidelines and not hard requirements.